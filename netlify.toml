[build]
  command = "node compile-data.js && npm install"
  publish = "."

[functions]
  directory = "netlify/functions"

[functions.node_bundle]
  # This matches the valid object syntax shown in your error log
